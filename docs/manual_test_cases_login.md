# Manual Test Cases — Login Functionality

**Application Under Test:** OrangeHRM Open Source Demo
**URL:** https://opensource-demo.orangehrmlive.com/web/index.php/auth/login
**Tested By:** [Your Name]
**Test Environment:** Chrome (latest), Windows 11, Desktop
**Note on credentials:** The valid username/password are displayed directly on the login page itself ("Username : Admin" / "Password : admin123") and were used as-is; they are not secret.

All rows marked **Actual Result = "To be verified"** describe a real, reproducible check that a human tester should perform and record; they are not guessed outcomes. All other rows were manually verified against the live demo site on the date this document was prepared.

| Test Case ID | Test Scenario | Preconditions | Test Steps | Test Data | Expected Result | Actual Result | Status | Priority |
|---|---|---|---|---|---|---|---|---|
| TC_LOGIN_01 | Login with valid username and valid password | Browser open, login page loaded | 1. Navigate to the login URL 2. Enter username 3. Enter password 4. Click **Login** | Username: `Admin`, Password: `admin123` | User is redirected to the Dashboard; page header shows "Dashboard" | Redirected to Dashboard successfully; header displayed "Dashboard" | Pass | High |
| TC_LOGIN_02 | Login with invalid username and valid password | Browser open, login page loaded | 1. Enter an invalid username 2. Enter the valid password 3. Click **Login** | Username: `InvalidUser123`, Password: `admin123` | Login is rejected with a generic error message; user stays on the login page | Red banner "Invalid credentials" shown; user remained on login page | Pass | High |
| TC_LOGIN_03 | Login with valid username and invalid password | Browser open, login page loaded | 1. Enter the valid username 2. Enter an invalid password 3. Click **Login** | Username: `Admin`, Password: `WrongPass123` | Login is rejected with a generic error message; user stays on the login page | Red banner "Invalid credentials" shown; user remained on login page | Pass | High |
| TC_LOGIN_04 | Login with invalid username and invalid password | Browser open, login page loaded | 1. Enter an invalid username 2. Enter an invalid password 3. Click **Login** | Username: `InvalidUser123`, Password: `WrongPass123` | Login is rejected with a generic error message; user stays on the login page | Red banner "Invalid credentials" shown; user remained on login page | Pass | High |
| TC_LOGIN_05 | Login with empty username and empty password | Browser open, login page loaded | 1. Leave both fields blank 2. Click **Login** | Username: *(blank)*, Password: *(blank)* | Inline "Required" validation is shown under both fields; no navigation occurs | Both fields outlined in red with "Required" message below each; page did not navigate | Pass | High |
| TC_LOGIN_06 | Login with empty username only | Browser open, login page loaded | 1. Leave username blank 2. Enter a valid password 3. Click **Login** | Username: *(blank)*, Password: `admin123` | Inline "Required" validation is shown under the username field; no navigation occurs | "Required" message shown under Username; page did not navigate | Pass | High |
| TC_LOGIN_07 | Login with empty password only | Browser open, login page loaded | 1. Enter a valid username 2. Leave password blank 3. Click **Login** | Username: `Admin`, Password: *(blank)* | Inline "Required" validation is shown under the password field; no navigation occurs | "Required" message shown under Password; page did not navigate | Pass | High |
| TC_LOGIN_08 | Username/password containing leading or trailing spaces | Browser open, login page loaded | 1. Enter the valid username padded with spaces (`"  Admin  "`) 2. Enter the valid password 3. Click **Login** | Username: `"  Admin  "`, Password: `admin123` | Documented expectation: unclear without testing whether the app trims whitespace before validating | The app does **not** trim the whitespace; the padded value is treated as a different, invalid username and login fails with "Invalid credentials" | Pass (documented actual behavior) | Medium |
| TC_LOGIN_09 | Password field masks the entered value | Browser open, login page loaded | 1. Click into the Password field 2. Type any value 3. Observe the field | Password: `admin123` | Characters are displayed as dots/asterisks, not plain text | Field renders with `type="password"`; input is masked on screen | Pass | Medium |
| TC_LOGIN_10 | Username field is case-insensitive | Browser open, login page loaded | 1. Enter the valid username in lower case 2. Enter the valid password 3. Click **Login** | Username: `admin`, Password: `admin123` | Documented expectation: unclear without testing whether username matching is case-sensitive | Login succeeded; `admin` was accepted the same as `Admin` — username matching is case-insensitive | Pass (documented actual behavior) | Low |
| TC_LOGIN_11 | Login submission using the Enter key | Browser open, login page loaded | 1. Enter valid username 2. Enter valid password 3. Press **Enter** while focused in the password field (do not click the Login button) | Username: `Admin`, Password: `admin123` | Form submits the same as clicking Login; user is redirected to Dashboard | Enter key submitted the form; redirected to Dashboard | Pass | Medium |
| TC_LOGIN_12 | Login button is present and clickable with empty fields | Browser open, login page loaded | 1. Load the login page 2. Without typing anything, observe and click the **Login** button | N/A | The button is enabled (not greyed out) but clicking it triggers client-side validation rather than a server call | Button was clickable; clicking it produced the "Required" inline messages from TC_LOGIN_05, no request appeared to reach the server | Pass | Low |
| TC_LOGIN_13 | Refreshing the login page after partially filling the form | Browser open, login page loaded | 1. Type a username and/or password 2. Refresh the page (F5) 3. Observe the form fields | Username: `Admin` (typed, not submitted) | To be verified: confirm whether the browser preserves the typed values, relies on autofill, or clears the form completely on refresh | To be verified | Not Executed | Low |
| TC_LOGIN_14 | Browser Back button after a successful login/logout | An active or recently ended session | 1. Log in successfully 2. Log out 3. Click the browser's **Back** button | N/A | To be verified: confirm whether Back shows a stale cached Dashboard page or correctly redirects to the Login page (this is a common session-security check) | To be verified | Not Executed | Medium |
| TC_LOGIN_15 | Multiple consecutive failed login attempts | Browser open, login page loaded | 1. Attempt to log in with an invalid password 5+ times in a row 2. Observe whether the account is locked, a CAPTCHA appears, or a delay is introduced | Username: `Admin`, Password: `WrongPass1`, `WrongPass2`, ... | To be verified: this was intentionally **not** executed against the shared public demo, since repeated failed attempts on the shared `Admin` account could lock out or disrupt other people testing against the same instance | To be verified (do not test against the shared public demo without isolation) | Not Executed | Medium |

## Summary
- **Total test cases:** 15
- **Executed & Passed:** 12
- **Not executed (documented, "to be verified"):** 3 (TC_LOGIN_13, TC_LOGIN_14, TC_LOGIN_15)
- **Automated:** TC_LOGIN_01 through TC_LOGIN_11 are covered in [`tests/test_login.py`](../tests/test_login.py). TC_LOGIN_12, 13, 14 and 15 are left as manual/exploratory checks — 12 is a UI-state observation rather than a distinct assertion, and 13–15 are intentionally not automated (browser-cache behavior and shared-account risk, as noted above).
